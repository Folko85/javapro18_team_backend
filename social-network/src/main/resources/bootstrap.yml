spring:
  profiles:
    active: ${SPRING_PROFILE:default}
  application:
    name: serv
  cloud:
    stream:
      rabbit:
        binder:
          nodes: 147.78.64.57:5672
          connection-name-prefix: serv
    config:
      uri: ${profile.config.url.${spring.profiles.active}}
      retry:
        max-attempts: 10
        initial-interval: 10000
        max-interval: 5000
    bus:
      enabled: true
      refresh:
        enabled: true
profile:
  config:
    url:
      default: http://localhost:8888
      prod: http://cloud-config:8888

