spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/network_db?allowPublicKeyRetrieval=true&useSSL=false&&serverTimezone=UTC
    username: user
    password: password
  liquibase:
    change-log: /db/changelog/changelog.yml
  jpa:
    show-sql: true
    hibernate:
      dialect: org.hibernate.dialect.MySQL8Dialect
      ddl-auto: validate

  mail:
    host: smtp.yandex.ru
    username: ivan.zukkel.mailsender@yandex.ru
    password: vdgclvhfzrqcjqvc
    port: 465
    protocol: smtps
mail:
  debug: true